import 'package:flutter/material.dart';

class ShareColors extends StatelessWidget {
  const ShareColors();
  @override
  Widget build(BuildContext context) => const Center(child: Icon(Icons.share, size: 100));
}
