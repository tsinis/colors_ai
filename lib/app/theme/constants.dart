import 'package:flutter/widgets.dart';

const Duration kDefaultTransitionDuration = Duration(milliseconds: 400);
const Duration kDefaultShortTransitionDuration = Duration(milliseconds: 300);
const Duration kDefaultReverseTransitionDuration = Duration(milliseconds: 200);
const Duration kDefaultLongTransitionDuration = Duration(seconds: 1);

const Curve kDefaultTransitionCurve = Curves.easeInOutCubicEmphasized;

const ShapeBorder kDefaultShape = RoundedRectangleBorder(borderRadius: BorderRadius.all(Radius.circular(28)));
